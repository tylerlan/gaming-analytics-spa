import React from 'react';

const MetricsOfTheDay = ({ section, pupdMetrics, onChangeSection }) => (
  <div>
    <p>
      helloh---------------------------------------------------------------------------------------------------------------------
    </p>
  </div>
);

// if any of the cards are clicked:
// onClick={e => onChangeSection('G')}
// change selected section to 'G', and go to the graph

// !!  should also change the currentMetric

export default MetricsOfTheDay;
